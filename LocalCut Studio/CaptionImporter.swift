import Foundation
import LocalCutCore

// CaptionImporterCompat was removed — it was dead code (never referenced).
// Production code uses CaptionImporter.parseLines(data:isVTT:) from LocalCutCore
// directly via EditorModel.importCaptionTrack(from:).
