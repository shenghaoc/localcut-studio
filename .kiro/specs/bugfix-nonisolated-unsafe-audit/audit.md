# Audit: Production `nonisolated(unsafe)`

Baseline: refreshed `origin/main` at `fc803fae487b9dd57b8a555a952c5505baecbc19`.

Required command:

```sh
rg "nonisolated\\(unsafe\\)" "LocalCut Studio" Packages
```

Count notes:

- Baseline raw `rg`/`git grep` text matches: **35**.
- Baseline production annotations: **31** across **13** files.
- Final raw `rg` text matches: **26**.
- Final production annotations: **22** across **10** files.
- Final `rg` output also includes explanatory comment lines; those are not
  counted as production annotations.
- `Packages/` has no production `nonisolated(unsafe)` annotations.

## Working Table

| File | Line/symbol | Owning type | Mutable state involved | Current access pattern | Current synchronization | Risk | Classification | Action | Justification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `LocalCut Studio/AudioMasterBus.swift` | origin/main:103 `liveSchedulingTask` | `AudioMasterBus` | Live scheduling `Task` handle | Main-actor schedule, cancel, teardown | `@MainActor` | low | safe because single-thread confined by documented lifecycle | remove | Runtime access is main-actor confined; normal isolation is sufficient. |
| `LocalCut Studio/AudioMasterBus.swift` | current:291 `comp` | `AudioMasterBus.scheduleLiveComposition` | `AVComposition` reference | Captured by detached decode task as a read-only snapshot | Main actor owns source; detached task only reads immutable snapshot | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | AVFoundation type is not Sendable, but the task does not mutate it and the comment names main-actor seek reuse. |
| `LocalCut Studio/AudioMasterBus.swift` | current:292 `mix` | `AudioMasterBus.scheduleLiveComposition` | `AVAudioMix` reference | Captured by detached decode task as a read-only snapshot | Main actor owns source; detached task only reads immutable snapshot | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Same confinement as `comp`. |
| `LocalCut Studio/ContentView.swift` | current:583 `previousDelegate` | `WindowConfigurator.Coordinator` | Weak `NSWindowDelegate` | Set/restored on main actor, read by Objective-C delegate forwarding | AppKit main-thread delegate lifecycle | low | framework interop escape hatch | keep with documented invariant | `responds(to:)` and `forwardingTarget(for:)` are Objective-C forwarding hooks that cannot express Swift actor isolation. |
| `LocalCut Studio/DiagnosticsAgent.swift` | current:107 `timer` | `DiagnosticsAgent` | `Timer` | Start/stop on main actor; invalidate in deinit | Main actor plus `Timer.invalidate()` teardown safety | low | framework interop escape hatch | keep with documented invariant | `deinit` is nonisolated, but the only off-actor access invalidates the timer. |
| `LocalCut Studio/EditorModel+AudioBus.swift` | current:150 `composition` | `EditorModel.measureLoudnessForProject` | `AVComposition` reference | Fresh build captured by detached loudness worker | Local read-only snapshot | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Heavy decode/DSP stays off the main actor; the local composition is consumed only by the worker. |
| `LocalCut Studio/EditorModel+AudioBus.swift` | current:151 `audioMix` | `EditorModel.measureLoudnessForProject` | `AVAudioMix` reference | Fresh build captured by detached loudness worker | Local read-only snapshot | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Same confinement as `composition`. |
| `LocalCut Studio/EditorModel.swift` | origin/main:155 `loudnessTask` | `EditorModel` | Loudness measurement `Task` handle | Main-actor cancel/assign/clear | `@MainActor` | low | safe because single-thread confined by documented lifecycle | remove | No nonisolated teardown access exists, so the unsafe annotation was unnecessary. |
| `LocalCut Studio/EditorModel.swift` | current:195 `recordingsFolderAccessURL` | `EditorModel` | Security-scoped recordings folder URL | Main-actor recording lifecycle; final deinit cleanup | Main-actor ownership; teardown-only nonisolated read | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | Needed only to balance a remaining security-scoped grant in nonisolated `deinit`. |
| `LocalCut Studio/EditorModel.swift` | current:201 `recordingMonitorTask` | `EditorModel` | Recording monitor `Task` handle | Main-actor assign/cancel; deinit cancellation | Main-actor ownership; idempotent task cancellation | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | Nonisolated `deinit` cannot otherwise cancel the task handle. |
| `LocalCut Studio/EditorModel.swift` | origin/main:192 `recordingPausedDuration` | `EditorModel` | Pause duration accumulator | Main-actor capture UI lifecycle | `@MainActor` | low | safe because single-thread confined by documented lifecycle | remove | Runtime access is main-actor confined; normal isolation is sufficient. |
| `LocalCut Studio/EditorModel.swift` | origin/main:194 `pauseStartedAt` | `EditorModel` | Pause start timestamp | Main-actor capture UI lifecycle | `@MainActor` | low | safe because single-thread confined by documented lifecycle | remove | Runtime access is main-actor confined; normal isolation is sufficient. |
| `LocalCut Studio/EditorModel.swift` | origin/main:204 `lastRecordingRequest` | `EditorModel` | Retake capture request | Main-actor capture UI lifecycle | `@MainActor` | low | safe because single-thread confined by documented lifecycle | remove | Runtime access is main-actor confined; normal isolation is sufficient. |
| `LocalCut Studio/EditorModel.swift` | current:239 `timeObserver` | `EditorModel` | AVPlayer time observer token | Install on main actor; remove in deinit | AVPlayer observer teardown | low | framework interop escape hatch | keep with documented invariant | Swift cannot express the nonisolated deinit cleanup path for this framework token. |
| `LocalCut Studio/EditorModel.swift` | current:244 `endObserver` | `EditorModel` | NotificationCenter observer token | Install on main actor; remove in deinit | NotificationCenter observer teardown | low | framework interop escape hatch | keep with documented invariant | Swift cannot express the nonisolated deinit cleanup path for this framework token. |
| `LocalCut Studio/EditorModel.swift` | current:248 `beatAnalysisTask` | `EditorModel` | Beat-analysis `Task` handle | Main-actor assign/cancel; deinit cancellation | Main-actor ownership; idempotent task cancellation | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | Needed only for final cancellation from nonisolated `deinit`. |
| `LocalCut Studio/EditorModel.swift` | current:257 `activeOverlaySourceRegistryID` | `EditorModel` | Overlay registry identifier | Main-actor preview install; deinit release | Main-actor ownership; teardown-only nonisolated read | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | The final read releases compositor overlay sources. |
| `LocalCut Studio/EditorModel.swift` | current:302 `accessedURLs` | `EditorModel` | Security-scoped media URLs | Main-actor import/document lifecycle; deinit cleanup | Main-actor ownership; teardown-only nonisolated iteration | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | Needed only to balance remaining security-scoped grants. |
| `LocalCut Studio/EditorModel.swift` | current:311 `bundleAccessURL` | `EditorModel` | Security-scoped bundle URL | Main-actor document lifecycle; teardown cleanup | Main-actor ownership; teardown-only nonisolated read | low | safe because single-thread confined by documented lifecycle | keep with documented invariant | Needed only to balance a remaining bundle grant. |
| `LocalCut Studio/EffectCompositor.swift` | origin/main:748 `overlaySourceLock` | `EffectCompositor` | Static `NSLock` binding | Static helper access | `NSLock` is Sendable; binding is immutable after initialization | low | unnecessary because state is immutable | remove | Converted to plain `private static let`; Swift no longer warns about an unnecessary unsafe annotation. |
| `LocalCut Studio/EffectCompositor.swift` | current:755 `overlaySourceRegistries` | `EffectCompositor` | Static registry dictionary | Preview/export register/release helpers | `overlaySourceLock` | medium | safe because lock-protected | keep with documented invariant | Mutable static dictionary remains protected by the lock for every access. |
| `LocalCut Studio/PreviewRebuildCoordinator.swift` | current:13 `pendingRebuildTask` | `PreviewRebuildCoordinator` | Debounce `Task` handle | Main-actor scheduling; teardown cancellation | Document lifecycle confines `cancelAll()` relative to scheduling | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Larger actor redesign is not needed while callers remain lifecycle-confined. |
| `LocalCut Studio/PreviewRebuildCoordinator.swift` | current:17 `activeRebuildTask` | `PreviewRebuildCoordinator` | Active rebuild `Task` handle | Main-actor scheduling; teardown cancellation | Same lifecycle confinement as `pendingRebuildTask` | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Same confinement as `pendingRebuildTask`. |
| `LocalCut Studio/ReconnectController.swift` | origin/main:9 `clock` | `ReconnectController` | Test clock closure | Test setup mutated before reconnect methods | None in baseline | medium | unsafe and locally fixable | remove | Replaced with immutable initializer injection, removing unsynchronized post-init mutation. |
| `LocalCut Studio/ReconnectController.swift` | origin/main:10 `sleep` | `ReconnectController` | Test sleep closure | Test setup mutated before reconnect methods | None in baseline | medium | unsafe and locally fixable | remove | Replaced with immutable initializer injection, removing unsynchronized post-init mutation. |
| `LocalCut Studio/RenderQueue.swift` | current:1097 `input` | `RenderQueue.pump` | `AVAssetWriterInput` | Captured by `requestMediaDataWhenReady` block | Single serial `pumpQueue` and sole pump ownership | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | The framework callback requires a Sendable block, but the writer input is confined to the serial pump. |
| `LocalCut Studio/RenderQueue.swift` | current:1098 `output` | `RenderQueue.pump` | `AVAssetReaderOutput` | Captured by `requestMediaDataWhenReady` block | Single serial `pumpQueue` and sole pump ownership | medium | safe because single-thread confined by documented lifecycle | keep with documented invariant | Same confinement as `input`. |
| `LocalCut Studio/ScreencastEventLogWriter.swift` | current:23 `localMonitor` | `ScreencastEventLogWriter` | Local NSEvent monitor token | Install/remove on main actor; deinit dispatches removal to main | AppKit main-thread monitor lifecycle | low | framework interop escape hatch | keep with documented invariant | Monitor removal must run on the AppKit thread, while `deinit` is nonisolated. |
| `LocalCut Studio/ScreencastEventLogWriter.swift` | current:27 `globalMonitor` | `ScreencastEventLogWriter` | Global NSEvent monitor token | Install/remove on main actor; deinit dispatches removal to main | AppKit main-thread monitor lifecycle | low | framework interop escape hatch | keep with documented invariant | Same invariant as `localMonitor`. |
| `LocalCut Studio/TimelineView.swift` | current:1192 `monitor` | `TimelineKeyHandlingView.Coordinator` | Local key monitor token | Install on main actor; deinit dispatches removal to main | AppKit main-thread monitor lifecycle | low | framework interop escape hatch | keep with documented invariant | Monitor lifetime is an Objective-C/AppKit callback token that cannot express Swift actor isolation. |
| `LocalCut Studio/VideoPublishTap.swift` | origin/main:32 `latestPixelBuffer` | `VideoPublishTap` | Latest captured pixel buffer | Write under lock; unprotected read in baseline | Existing `NSLock`, but baseline only protected writes | medium | unsafe and locally fixable | protect with lock | Replaced the unsafe stored property with a lock-protected computed getter and private backing storage. |
| `LocalCut Studio/WhipSession.swift` | origin/main:70 `_cachedStateStream` | `WhipSession` | Cached state stream | Actor-isolated access through `stateStream` | Actor isolation | low | safe because single-thread confined by documented lifecycle | remove | Actor isolation is sufficient; the unsafe annotation was unnecessary. |

## Follow-up PRs

No high-risk retained annotations remain and no larger refactor is required by
this audit. If `PreviewRebuildCoordinator.cancelAll()` is ever called from
arbitrary concurrent executors instead of teardown paths, promote the coordinator
to an explicitly synchronized or actor-isolated design in a separate PR.
